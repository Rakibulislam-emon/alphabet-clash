/** @type {import('tailwindcss').Config} */
module.exports = {
    content: ["./src/**/*.{html,js}"],
    theme: {
      extend: {},
    },
    plugins: [],
  }
   
        // // score update
        // // 1. get the current score 
        // const currentScore =document.getElementById('currentScore');
        // const innerTextOfScore = currentScore.innerText;
        // const convertToInt = parseInt(innerTextOfScore);
        // console.log(convertToInt);
        // // 2.increase the score
        // const newScore = convertToInt + 1;
        // // show the updated score
        // currentScore.innerText = newScore;   
        // // started a new round

        
  //  const currentAlphabet = getElementsInnerTextValueById('alphabets')
  //  console.log(currentAlphabet)

  
// function getElementsInnerTextValueById(elemnt){
//   const elemnt = document.getElementById(elemnt);
//   const text = elemnt.innerText;
//   return text;
// }